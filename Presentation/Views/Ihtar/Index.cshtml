@model IEnumerable<Entity.Dto.IhtarDto>

@{
    ViewBag.Title = "İhtar Listesi";
}

<h2>İhtarlar</h2>

<tr>
    <td colspan="6">
        <div style="display:flex; justify-content:flex-end;">
            <button type="button" class="btn btn-success"
                    data-bs-toggle="modal"
                    data-bs-target="#createModal">
                İhtar Ekle
            </button>
        </div>
    </td>
</tr>

<table class="table table-striped">
    <thead>
        <tr>
            <th><input type="checkbox" id="selectAll" /></th>
            <th>Müşteri</th>
            <th>Borç Tutarı</th>
            <th>İhtar Tarihi</th>
            <th>Şube</th>
            <th>Avukat</th>
            <th>Ürünler</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var ihtar in Model)
        {
            <tr>
                <td><input type="checkbox" class="rowCheckbox" value="@ihtar.IhtarId" /></td>
                <td>@ihtar.MusteriAd</td>
                <td>@ihtar.BorcTutar</td>
                <td>@ihtar.IhtarTarih.ToString("dd.MM.yyyy")</td>
                <td>@ihtar.SubeAd</td>
                <td>@ihtar.AvukatAd</td>
                <td>
                    @if (ihtar.UrunId > 0)
                    {
                        <span>@ihtar.UrunAd</span>
                    }
                    else
                    {
                        <span class="text-muted">Ürün yok</span>
                    }
                </td>
                <td>
                    <button class="btn btn-sm btn-warning updateBtn" data-id="@ihtar.IhtarId">Güncelle</button>
                    <button class="btn btn-sm btn-danger deleteBtn" data-id="@ihtar.IhtarId">Sil</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@Html.Partial("_IhtarModals", new Entity.Dto.IhtarDto())

@section Scripts {
    <script src="~/Scripts/crud.js"></script>
    <script>
        initCrud("Ihtar", ["IhtarId", "MusteriId", "SubeId", "AvukatId", "BorcTutar", "IhtarTarih", "UrunId"]);
    </script>
}
